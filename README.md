# 🏥 AI X-Ray Analysis on Blockchain

ระบบจำลอง Blockchain สำหรับจัดเก็บผลวิเคราะห์ภาพถ่ายทางการแพทย์ (X-Ray) โดยใช้ AI ร่วมกับเทคโนโลยี Blockchain เพื่อความปลอดภัย ความโปร่งใส และป้องกันการแก้ไขข้อมูล (Immutability)

โปรเจกต์นี้พัฒนาด้วยภาษา **Python** โดยใช้ **FastAPI** เป็น Backend และจำลองกลไก Blockchain แบบ **Proof of Reputation & Validity (PoRV)**

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```text
assignment-1/
│
├── models.py        # Data Structures (Transaction, Block, Node)
├── blockchain.py    # Core Logic (Consensus, Mining, Hashing)
├── main.py          # API Endpoints (FastAPI Controller)
└── requirements.txt # Dependencies list
```

---

## ⚙️ วิธีการติดตั้งและเริ่มใช้งาน (Installation & Setup)

กรุณาทำตามขั้นตอนทีละข้อ:

### 1️⃣ เข้าสู่โฟลเดอร์โปรเจกต์

เปิด Terminal หรือ Command Prompt แล้วพิมพ์คำสั่ง:

```bash
cd blockchain
```

---

### 2️⃣ สร้าง Virtual Environment (ทำครั้งแรกครั้งเดียว)

เพื่อแยกสภาพแวดล้อมในการติดตั้ง Library ของโปรเจกต์นี้โดยเฉพาะ

**สำหรับ Windows:**

```bash
python -m venv venv
```

**สำหรับ Mac / Linux:**

```bash
python3 -m venv venv
```

---

### 3️⃣ เปิดใช้งาน (Activate) Virtual Environment ⚠️ *สำคัญมาก*

ต้องทำขั้นตอนนี้ทุกครั้งก่อนรันโปรแกรม เพื่อให้ระบบใช้ Library จาก Virtual Environment ที่ถูกต้อง

**สำหรับ Windows:**

```bash
venv\Scripts\activate
```

> หากใช้ PowerShell แล้วพบ Error ตัวอักษรสีแดง ให้รันคำสั่งด้านล่างก่อน แล้วลอง Activate ใหม่อีกครั้ง
>
> ```powershell
> Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
> ```

**สำหรับ Mac / Linux:**

```bash
source venv/bin/activate
```

📌 *สังเกต:* เมื่อ Activate สำเร็จ จะมีคำว่า `(venv)` แสดงอยู่ด้านหน้าบรรทัดคำสั่ง

---

### 4️⃣ ติดตั้ง Library ที่จำเป็น (ทำครั้งแรกครั้งเดียว)

```bash
pip install -r requirements.txt
```

---

## ▶️ วิธีการรันระบบ (Run Server)

เมื่อ Activate Virtual Environment เรียบร้อยแล้ว (`(venv)` แสดงอยู่) ให้รันคำสั่ง:

```bash
uvicorn main:app --reload
```

หากรันสำเร็จ จะปรากฏข้อความ:

```text
INFO:     Uvicorn running on http://127.0.0.1:8000
```

จากนั้นสามารถเปิด Browser และเข้าใช้งาน API ได้ที่:

* 🌐 [http://127.0.0.1:8000](http://127.0.0.1:8000)
* 📘 Swagger UI (API Documentation): [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

---

## ✅ หมายเหตุเพิ่มเติม

* โปรเจกต์นี้เป็น **Prototype / Simulation** สำหรับการศึกษาแนวคิด AI + Blockchain
* ไม่เหมาะสำหรับใช้งานจริงทางการแพทย์ (Not for clinical use)
* สามารถต่อยอดเพิ่ม Model AI หรือเชื่อมต่อ IPFS / Blockchain จริงได้ในอนาคต

---

📌 *Developed for educational and research purposes*
